ext.COMPILE_SDK_VERSION="3.3.0"
ext.BUILD_TOOLS_VERSION="3.3.0"
ext.MIN_SDK_VERSION="3.3.0"
ext.TARGET_SDK_VERSION="3.3.0"

System.getProperties().put("COMPILE_SDK_VERSION", COMPILE_SDK_VERSION)
System.getProperties().put("BUILD_TOOLS_VERSION", BUILD_TOOLS_VERSION)
System.getProperties().put("MIN_SDK_VERSION", MIN_SDK_VERSION)
System.getProperties().put("TARGET_SDK_VERSION", TARGET_SDK_VERSION)
